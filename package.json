{
  "name": "wolf",
  "version": "1.0.0",
  "description": "No javascript. Pure (s)css",
  "main": "index.html",
  "scripts": {
    "test": "echo \"test\"",
    "sass" : "node-sass scss/main.scss --output css",
    "watch-sass": "npm run sass -- --watch",
    "build": "cp index.html docs && cp assets/* docs/assets && node-sass scss/main.scss --output docs/css"
  },
  "author": "niC00L",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.6.1"
  }
}
